system_info:
  default_user:
    name: opc
    lock_passwd: true
    gecos: OCI default user
    groups: [adm, systemd-journal, wheel, sudo]
    sudo: ["ALL=(ALL) NOPASSWD:ALL"]
    shell: /bin/bash
users:
  - default